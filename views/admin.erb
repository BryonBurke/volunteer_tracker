<h2>All Projects</h2>
<% if @projects.any? %>
<ul>
  <% @projects.each do |project| %>
    <li><a href="/home/admin/projects/<%= project.id %>"><%= project.title %></a></li>
  <% end %>
</ul>
<% else %>
<p>There are currently no projects to display.</p>
<% end %>

<a href="/home/admin/projects/new">Add a new project</a>
